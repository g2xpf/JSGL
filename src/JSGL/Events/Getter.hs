module JSGL.Events.Getter (
) where

import qualified Graphics.UI.GLFW as GLFW
import JSGL.Types.GS
import JSGL.Types.Type

--getKeyState :: Key -> GS s KeyState
--getKeyState key = do
--  window <- getWindow <$> getMetaInfo
--  liftIO $ GLFW.getKey key
--
--(|==.3) :: Key -> KeyState -> GS s Bool
--key :== keyState = do 
--  keyState' <- getKeyState key
--  return $ keyState == keyState'
  
